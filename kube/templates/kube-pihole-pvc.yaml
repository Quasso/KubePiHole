---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ${PIHOLE_NAME}-local-etc-claim
spec:
  storageClassName: local
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  selector:
    matchLabels:
      directory: etc